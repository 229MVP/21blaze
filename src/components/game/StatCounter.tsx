
import React from 'react'; import { View,Text,StyleSheet } from 'react-native'; import { BlazePanel } from '../ui/BlazePanel'; import { colors,spacing,typography } from '../../theme/uiKit';
export function StatCounter({label,value,accent=false,danger=false,compact=false,accessibilityValue}:{label:string;value:string|number;accent?:boolean;danger?:boolean;compact?:boolean;accessibilityValue?:string}){return <BlazePanel padding={compact?spacing.sm:spacing.md} style={styles.panel}><View accessible accessibilityLabel={`${label}: ${accessibilityValue??value}`}><Text style={styles.label}>{label}</Text><Text style={[styles.value,accent&&styles.accent,danger&&styles.danger]}>{value}</Text></View></BlazePanel>}
const styles=StyleSheet.create({panel:{flex:1,minWidth:64},label:{color:colors.text.secondary,fontFamily:typography.families.condensed,fontSize:10,textAlign:'center',letterSpacing:.8},value:{color:colors.fire.gold,fontFamily:typography.families.condensed,fontWeight:'800',fontSize:18,textAlign:'center',marginTop:4},accent:{color:colors.fire.brightOrange},danger:{color:colors.status.danger}});
